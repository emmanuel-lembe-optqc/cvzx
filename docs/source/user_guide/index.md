# User guide

These guides are task-oriented and example-driven — every code snippet is adapted directly
from the test suite (`tests/`), so if something here looks unclear or out of date, the
matching test is the ground truth to check against.

```{toctree}
:maxdepth: 1

quickstart
gates
rewrite_rules
optimization
circuit_conversion
visualization
```
